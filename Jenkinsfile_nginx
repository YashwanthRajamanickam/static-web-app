pipeline{
    agent any
    environment {
    nginx_IP = '15.206.73.132'
    }

    triggers {
  githubPush()
}
  options {
  buildDiscarder logRotator(numToKeepStr: '5')
}

        stages{
        //     stage('removing old files'){
        //         steps{
        //         sshagent(['Nginx']) {
        //             sh "ssh -o StrictHostKeyChecking=no ec2-user@${nginx_IP} 'sudo rm -rf /usr/share/nginx/html/*'"
        //         }
        //         }
        // }
        
        //     stage('cloning the repo'){
        //         steps{
        //         git branch: 'development', credentialsId: 'Yashwanth_Studentwebapp', url: 'https://github.com/YashwanthRajamanickam/static-web-app.git'
        //         }
            
        // }

           stage('copying the folder'){
                steps{
                sshagent(['Nginx']){
                sh """
                scp -o StrictHostKeyChecking=no static-web-app/* ec2-user@${nginx_IP}:/usr/share/nginx/html/
                sleep 15
                ls /usr/share/nginx/html/ 
                """
                }
                 }
            
    
    }
}